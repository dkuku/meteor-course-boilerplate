import React, { Component } from 'react';

export default const NotesListEmptyItem = (props) => {
    return (
        <div>
            <p>Your notes list is currently empty</p>
        </div>
    )
}
